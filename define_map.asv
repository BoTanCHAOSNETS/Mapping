function [layout_grid] = define_map(x_range,y_range,resolution)

x_vector = [1 : resolution : x_range];
y_vector = [1 : resolution : y_range];

grid_size = ;
layout_grid = cell(numel(x_vector));

for k = 1:
end